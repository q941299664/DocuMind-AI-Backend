<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.4</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>

    <groupId>com.documind</groupId>
    <artifactId>documind-dependencies</artifactId>
    <version>${revision}</version>
    <packaging>pom</packaging>

    <properties>
        <revision>1.0.0-SNAPSHOT</revision>
        <documind.version>${revision}</documind.version>
        <spring-cloud.version>2023.0.1</spring-cloud.version>
        <spring-cloud-alibaba.version>2023.0.1.0</spring-cloud-alibaba.version>
        <spring-cloud-starter-bootstrap.version>5.0.1</spring-cloud-starter-bootstrap.version>
        <hutool.version>5.8.26</hutool.version>
        <mybatis-plus.version>3.5.9</mybatis-plus.version>
        <mybatis-spring.version>3.0.3</mybatis-spring.version>
        <druid.version>1.2.21</druid.version>
        <knife4j.version>4.5.0</knife4j.version>
        <jjwt.version>0.12.5</jjwt.version>
        <lombok.version>1.18.42</lombok.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Spring Cloud -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Spring Cloud Alibaba -->
            <dependency>
                <groupId>com.alibaba.cloud</groupId>
                <artifactId>spring-cloud-alibaba-dependencies</artifactId>
                <version>${spring-cloud-alibaba.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- DocuMind Common Modules -->
            <dependency>
                <groupId>com.documind</groupId>
                <artifactId>documind-common-core</artifactId>
                <version>${documind.version}</version>
            </dependency>
            <dependency>
                <groupId>com.documind</groupId>
                <artifactId>documind-common-web</artifactId>
                <version>${documind.version}</version>
            </dependency>
            <dependency>
                <groupId>com.documind</groupId>
                <artifactId>documind-common-database</artifactId>
                <version>${documind.version}</version>
            </dependency>
            <dependency>
                <groupId>com.documind</groupId>
                <artifactId>documind-common-redis</artifactId>
                <version>${documind.version}</version>
            </dependency>
            <dependency>
                <groupId>com.documind</groupId>
                <artifactId>documind-common-security</artifactId>
                <version>${documind.version}</version>
            </dependency>
            <dependency>
                <groupId>com.documind</groupId>
                <artifactId>documind-common-swagger</artifactId>
                <version>${documind.version}</version>
            </dependency>

            <!-- DocuMind API Modules -->
            <dependency>
                <groupId>com.documind</groupId>
                <artifactId>documind-system-api</artifactId>
                <version>${documind.version}</version>
            </dependency>
            <dependency>
                <groupId>com.documind</groupId>
                <artifactId>documind-document-api</artifactId>
                <version>${documind.version}</version>
            </dependency>
            <dependency>
                <groupId>com.documind</groupId>
                <artifactId>documind-ai-api</artifactId>
                <version>${documind.version}</version>
            </dependency>
            <dependency>
                <groupId>com.documind</groupId>
                <artifactId>documind-search-api</artifactId>
                <version>${documind.version}</version>
            </dependency>

            <!-- Third Party Libs -->
            <dependency>
                <groupId>cn.hutool</groupId>
                <artifactId>hutool-all</artifactId>
                <version>${hutool.version}</version>
            </dependency>

            <!-- DB -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-boot-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>org.mybatis</groupId>
                <artifactId>mybatis-spring</artifactId>
                <version>${mybatis-spring.version}</version>
            </dependency>
            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>druid-spring-boot-starter</artifactId>
                <version>${druid.version}</version>
            </dependency>

            <!-- Swagger -->
            <dependency>
                <groupId>com.github.xiaoymin</groupId>
                <artifactId>knife4j-openapi3-jakarta-spring-boot-starter</artifactId>
                <version>${knife4j.version}</version>
            </dependency>

            <!-- JWT -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
            </dependency>

            <!-- Spring Cloud Bootstrap -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-starter-bootstrap</artifactId>
                <version>${spring-cloud-starter-bootstrap.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

</project>


